server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chienlv03/Agricultural-product-market.git
          search-paths: config-repo
          default-label: main